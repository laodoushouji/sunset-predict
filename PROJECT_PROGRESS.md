# Sunset Predict 项目进展与需求变更总览

> 更新时间：2026-08-14 CST
> 生产环境：[https://sunsetpredict.cloud/](https://sunsetpredict.cloud/)
> 当前阶段：多站点 MVP / 公开试运行 + GEO（生成式引擎优化）基础落地
> 最近部署：`20260814T012845Z`（GEO P1：llms.txt + /about 方法论页 + JSON-LD 扩展 + 全站页脚引用声明 + 城市/省份 37 个落地页 + 自托管字体，已上线）

## 1. 项目结论

Sunset Predict 已经从“西湖单站双卡片原型”发展为一个包含 19 个摄影站点、实时气象 API、日期切换、详情抽屉、摄影建议、赞赏与合作入口、SEO 基础设施的可公开访问产品。

当前最重要的结论不是继续增加页面功能，而是进入“算法收敛与数据校准”阶段：

- 页面、交互、部署链路已经基本成型。
- 西湖、外滩与 17 个全国站点均已接入实时模型。
- 质量分与观测成功率已经严格分轨，Quality V3 已改为连续画布分、零能见度保底和固定景观加分。
- 全部 19 个站点均已接入降水量、降水概率、天气代码和统一降雨阻断规则。
- 历史快照和匿名实况反馈机制已经存在，但样本仍处于起步阶段，暂时不能做可靠的 90 天统计校准。
- 本地 Git 基线和 V3 功能提交已经建立，生产部署仍保留备份、健康检查和失败回滚。

## 2. 当前完成度概览

| 领域 | 状态 | 说明 |
|---|---|---|
| 首页视觉与移动端 | 已完成 | 双列毛玻璃卡片、手机端左右排列、卡片等高、背景图本地化 |
| 19 个摄影站点 | 已完成 | 西湖、外滩和 17 个全国站点均有实时卡片与详情抽屉 |
| 日期切换 | 已完成 | 支持昨天、今天、明天、后天；昨天依赖服务器快照 |
| 详情抽屉 | 已完成 | Bottom Sheet、遮罩、下滑关闭、URL Hash 分享、全站点复用 |
| 评分与概率双轨 | 已完成 | V3 同时输出 `rawQuality`、`quality` 与独立 Probability |
| 景区差异化算法 | 已完成 | 10 站正向景观修正均为固定小分，累计上限 10 分 |
| 降雨与天气 | 已完成 | 10 站均返回天气、降水强度和概率；中雨及以上评分与成功率均为 0 |
| 历史校准 | 部分完成 | 已上线“是否看到 / 实际质量”匿名反馈；目前尚无足够样本进行统计校准 |
| 微信赞赏 | 已完成 | 页面底部只保留微信赞赏 |
| 商业合作位 | 部分完成 | 前端卡片与二维码已完成，暂无广告后台或投放配置服务 |
| SEO 基础 | 已完成 | Meta、语义化 HTML、robots.txt、sitemap.xml 已上线 |
| SEO 内容扩展 | 已完成 | 19 城 + 18 省共 37 个 `/spots/*` 落地页已上线，sitemap.xml 扩至 38 条 `<loc>`，含原创摄影指南与直给摘要 |
| GEO / AIO 基础 | 已完成 | llms.txt、/about 方法论页、落地页 Dataset/Article JSON-LD、全站页脚引用授权声明已上线 |
| 运维部署 | 已完成 | 阿里云香港服务器、Nginx、systemd、HTTPS、自动备份与回滚 |
| 源码版本管理 | 已完成 | 已建立 40 个受管文件的首个基线，并单独提交 V3 变更 |
| 项目文档一致性 | 已完成 | README 已更新为阿里云 Nginx + Node + Open-Meteo 实际架构 |

## 3. 已上线站点

首页共展示 19 个站点：2 个核心卡片 + 17 个全国摄影站。

| 站点 | Spot ID | 西方窗口 | 已实现的差异化逻辑 | 数据缺口 |
|---|---|---:|---|---|
| 杭州西湖 | `xihu` | 临安约 38km，另有富阳辅助点 | 山脊提前 15 分钟、湖面反射 +2、动态机位、降雨阻断 | 暂无实拍校准 |
| 上海外滩 | `waitan` | 青浦近程 + 苏州/无锡/常州远程约 150km | AQI 色彩、远程破窗、建筑与江面反光 +2 | 暂无实拍校准 |
| 北京景山 / 故宫 | `beijing` | 门头沟约 50km | 山云遮挡、干燥金橙色、通透度强化 | 暂无实拍校准 |
| 大理洱海 | `erhai` | 苍山约 15km | 近程山体阻断、高海拔固定 +4、水面色彩 | 暂无实拍校准 |
| 重庆来福士 / 南山 | `chongqing` | 永川方向约 100km | 低能见度阈值、高湿、两江反射 | 暂无实拍校准 |
| 厦门黄厝沙滩 | `xiamen` | 漳州方向约 100km | 海雾、低层湿度、台风外围预留 | 未接入真实台风路径 |
| 青岛栈桥 / 五四广场 | `qingdao` | 潍坊方向约 100km | 东南海风、海滨霾、余晖慢衰减 | 暂无实拍校准 |
| 成都金融城双塔 | `chengdu` | 雅安 / 贡嘎方向约 150km | 盆地破窗、12km 能见度满分、粉紫色倾向 | 旧厚云函数存在断崖式变化 |
| 深圳人才公园 / 深圳湾 | `shenzhen` | 中山方向约 100km | 建筑金光 +3、海雾、台风外围预留 | 未接入真实台风路径 |
| 黄山光明顶 | `huangshan` | 池州方向约 100km | 925/700hPa 云海代理、雾中阻断、高空风速 | 无直接云底高度，当前使用压力层代理 |

## 4. 需求变更记录

### 4.1 首页视觉与卡片

- 初始需求：使用 HTML5、Tailwind CSS、ES6、Lucide Icons 构建双列毛玻璃卡片。
- 西湖卡片从静态展示升级为动态分数、等级、概率、进度条和分数颜色。
- 外滩最初为灰色锁定卡片，后来改为正式解锁并接入实时数据。
- 手机端确定为左右双列排列，视觉参考小红书双列流。
- 西湖与外滩卡片已调整为相同尺寸和对齐结构。
- 背景图从外部 Wikimedia 链接改为服务器本地资源，减少国内加载失败。
- 19 个站点均使用各自标志性照片作为卡片底色。
- 洱海、青岛、厦门图片根据反馈更换过一轮。
- `LIVE FORECAST` 已移动到右上角。
- 西湖卡片上的“银灰雾霭”色彩标签已整体移除。
- 外滩“算法调校中 / 试运行仅供参考”等技术文案已移除或精简。

### 4.2 首页模块删改

- “三日趋势”模块已删除。
- 页面底部可见的图片来源行已删除；版权信息保留在独立 `/credits` 页面。
- “今日判断”模块已删除。
- “昨日实况”标题、占位内容及相关样式已删除。
- 日期切换中的“昨天记录”仍然保留，它与已删除的“昨日实况照片”不是同一个功能。

### 4.3 日期滑动与历史

- 日期切换组件位于西湖 / 外滩卡片下方。
- 支持按钮、触摸手势和整页横向滑动语义：
  - 向右滑：今天 → 明天 → 后天。
  - 向左滑：后天 → 明天 → 今天 → 昨天。
- 后端 `/api/timeline` 统一西湖、外滩与全国站点的日期。
- 服务器会保存每日快照，用于显示昨天数据。
- 历史文件已升级为 schema v2，并按站点保存 `inputs`、`adjustments`、`components` 与 `outputs`。
- `outputs` 同时记录 `rawQuality`、`quality`、`probability`、等级和概率等级；旧版无 schema 文件仍可读取。
- 当前服务器历史目录只有 1 个 JSON 文件，因此“昨天”能力刚开始积累数据。

### 4.4 详情抽屉

- 所有 19 个站点共用底端抽屉式详情面板。
- 点击卡片后从底部平滑滑入。
- 支持遮罩点击关闭、关闭按钮、Esc 和向下拖动关闭。
- 抽屉打开时使用 `#!spotId` 更新 URL，可直接分享对应站点。
- 第一屏包含评分、等级、建议机位、日落时间和相机参数。
- 下方包含高 / 中 / 低云、画布、滤镜、光源、色彩、置信度、算法点评和作者碎碎念。
- 西湖建议机位可根据日落方位角动态变化。
- 所有站点均使用各自的高质量、低质量高概率、低概率遮挡文案。
- 详情物理层增加天气状况、每小时降水强度和降水概率。
- 商业合作卡片保留在抽屉底部。
- 所有站点抽屉增加“今晚是否看到 / 实际质量”反馈，今天与历史日期始终开放，未来日期不可提交。

### 4.4.1 实况反馈与校准真值

- `observed` 作为 Probability 的观测真值，记录今晚是否实际看到晚霞。
- `actualQuality` 作为 Quality 的质量真值，采用平淡、微霞、不错、很棒、爆燃五档。
- 服务端在提交时绑定并保存当日预测的 Quality、Probability、等级、天气、组件、指标与模型来源，避免后续算法升级后丢失对照依据。
- 同一匿名设备、同一站点、同一日期重复提交会更新同一条记录，不重复累计样本。
- 服务端只保存匿名标识的 SHA-256 摘要，反馈记录不包含原始设备标识、IP 或个人信息。
- 反馈以 SQLite 单文件存储于 `/var/lib/sunset-predict/feedback.db`（WAL 模式，位于发布目录之外），版本发布和自动回滚不会覆盖反馈数据；旧版按日期目录存放的 JSON 可经 `npm run migrate:feedback` 一次性迁移。

### 4.5 分数视觉

- 分数颜色已经按区间动态切换：
  - `0–29`：蓝色 / 冷色。
  - `30–59`：紫色 / 粉色。
  - `60–84`：橙色 / 金色。
  - `85–100`：红色 / 深橙色。
- FIRE 分数支持进度光晕脉动效果。
- 每个站点卡片同时显示 Quality 与观测成功率。
- 低成功率时卡片会变暗，高分高成功率时增强暖色和发光效果。

### 4.6 Quality 与 Probability 双轨模型

当前 V3 实现：

- API、首页和详情抽屉均分别输出 `quality` 与 `probability`。
- 远端低云、本地低云主要影响观测成功率，不再普遍混入质量分。
- 前端使用四象限逻辑生成“爆燃预警 / 极致但危险 / 清透落日 / 建议休息”。
- Probability 当前采用正确的加权方向：远端通光率 × 0.8 + 本地通光率 × 0.2。
- 远端云墙、本地低云、低层湿度等规则可对 Probability 设上限或倍率。
- 画布分为 `canvasCoverage × 0.8`，综合云量超过 85% 后使用连续厚云衰减，100% 厚云约为 32 画布分。
- 能见度 ≤5km 时滤镜分为 0，随后线性增长到各城市满分阈值。
- 250hPa 湿度修正改为固定 `+1 / -5`，不再使用百分比放大。
- 所有正向景观修正使用固定分，并由统一上限器保证累计 `sceneBonus ≤ 10`。
- 阴天不再统一截断为 59 分；改用中高云覆盖率与 250hPa 湿度估算光学厚度，并连续衰减质量分。
- API 同时输出原始物理分 `rawQuality` 与展示分 `quality`；当前为恒等映射，未来有足够真值后再校准展示分。
- 生产模型版本为 `quality-v3.1`；西湖来源为 `xihu-model-v3`，外滩为 `waitan-model-v4`，全国站为 `*-model-v3`。
- 2026-07-17 生产抽样已出现 `成都 86/9` 与 `北京 7/100`，证明高质量低成功率、低质量高成功率能够独立保留。

### 4.7 全站天气与降雨规则

西湖、外滩与 17 个全国站点的 Open-Meteo 主数据均已增加：

- `precipitation`
- `precipitation_probability`
- `rain`
- `weather_code`

西湖同时读取小时级与 15 分钟级数据，其余站点读取日落小时数据，并统一为 `mm/h`。

当前规则：

- 小雨：保留潜在质量分，`probability ≤ 10`。
- 中雨、大雨、雷雨及中雪以上阻断天气：`quality = 0`，`probability = 0`。
- 无雨或雨已停止：恢复正常评分。

首页所有卡片右上角显示“晴 / 多云 / 阴 / 雾 / 小雨 / 中雨 / 大雨 / 雷雨”等状态；详情抽屉显示天气、降水强度和降水概率。天气代码同时参与分级，避免只靠毫米数漏判阵雨或雷雨。

注意：这是对“Quality 与 Probability 严格分轨”的产品级例外。最终决定是：强降雨时用户不应看到高质量分。

### 4.8 数据来源

- 西湖：Open-Meteo 主点（含小时与 15 分钟降水）+ 临安 / 富阳窗口 + 杭州 WAQI。
- 外滩：Open-Meteo 主点（含降水）+ 青浦近程窗口 + 150km 远程窗口 + 上海 WAQI。
- 全国 16 站：Open-Meteo 本地点（含降水）+ 按日落方位角生成的西方窗口。
- 黄山：用 925hPa 高湿 + 700hPa 干燥代理云海条件。
- WAQI Token 只存在服务器环境变量和本地 `.env`，未写入前端或代码。

早期文档中出现过 QWeather、Cloudflare Worker `/api/forecast`、`/api/predict` 和 `/api/spot/xihu` 规划，但这些地址当前没有实现。线上西湖正式入口是 `/sunset`。

### 4.9 赞赏与商业合作

- 旧“请喝咖啡 / Buy me a coffee”文案已移除。
- 支持区改为“为浪漫续航”。
- 支付方式只保留微信。
- 支持区位于页面最下方，并已归入语义化 `<footer>`。
- 商业合作卡片位于详情抽屉底部。
- 合作方向：咖啡、住宿、器材租赁、摄影服务。
- `business.jpg` 已作为合作二维码上线。
- `advertiserData` 前端占位逻辑已经存在，但没有后台管理、定向投放或数据统计。

### 4.10 SEO、AIO 与语义化

已完成：

- Title：`晚霞预测 - 今日火烧云概率与摄影指南 | Sunset Predict`
- Description 与 Keywords 已按晚霞预测、西湖、外滩、摄影机位等关键词优化。
- 核心西湖分数是页面唯一 `<h1>`。
- 摄影指南、昨日实况等标题层级完成过语义化；昨日实况后来整体删除。
- 主体使用 `<main>`。
- 赞赏与版权信息使用 `<footer>` 包裹。
- 所有静态和动态图片均有描述性 `alt`。
- Lucide 装饰图标使用 `aria-hidden`，功能按钮使用 `aria-label`。
- `/robots.txt` 已上线。
- `/sitemap.xml` 已上线，扩至 38 条 `<loc>`（首页 + `/credits` + 37 个 `/spots/*` 落地页）。
- `llms.txt` 已上线（`/llms.txt`，面向 AI 爬虫的站点地图）。
- `/about` 方法论页已上线（定义“晚霞预测”、Quality V3 双维度、术语表、引用授权）。
- 落地页 JSON-LD 已扩展 `Dataset`（晚霞质量分/观测成功率/最佳观测窗口）+ `Article`（author/datePublished/dateModified）+ `Organization`。
- 全站页脚「欢迎在注明出处前提下引用预测数据」声明已上线，链接 `/about`。
- 19 个城市 + 18 个省份独立落地页已完成（共 37 个 `/spots/*` URL），含直给摘要与原创摄影指南。

尚未完成（依赖用户平台操作，非代码）：

- 尚未在 GSC 配置 `GOOGLE_SITE_VERIFICATION` / 百度 `BAIDU_SITE_VERIFICATION` meta（静态托管的 `google644a617b7e117520.html` 验证文件已通过，但页面 meta 仍需 env 注入）。
- 尚未在 Google / Bing / 百度主动提交 `sitemap.xml`；验证文件已 OK，但收录面仍小，长尾覆盖能力就绪待平台提交后扩大。

### 4.11 部署与运维

- 生产服务器：阿里云香港节点。
- 域名：`sunsetpredict.cloud`。
- HTTPS 已启用。
- Nginx 负责域名、静态路由与 Node 反向代理。
- systemd 服务：`sunset-predict.service`。
- Node 服务监听 `127.0.0.1:3003`。
- 部署脚本会：
  1. 备份当前 Nginx、systemd 服务和旧发布目录。
  2. 上传新前端与后端。
  3. 写入服务器环境变量文件。
  4. 重启后端并等待健康检查。
  5. 校验页面、API、二维码、图片、SEO 文件和 HTTPS。
  6. 任一步失败时自动回滚。
- 历史快照与实况反馈均位于发布目录之外，部署和回滚不会清空校准数据。

## 5. 当前线上接口

以下接口在 2026-07-17 验证均返回 HTTP 200：

| 地址 | 用途 |
|---|---|
| `/` | 首页与 10 站卡片 |
| `/sunset` | 西湖三日预测与当前详情 |
| `/api/spot/waitan` | 外滩预测 |
| `/api/spots` | 17 个全国站点聚合预测 |
| `/api/spot/:slug` | 全国单站详情；支持 `jingshan` → `beijing` 别名 |
| `/api/timeline` | 昨天 / 今天 / 明天 / 后天统一时间线 |
| `POST /api/feedback` | 提交“是否看到 / 实际质量”匿名校准真值 |
| `/credits` | 图片版权说明 |
| `/robots.txt` | 爬虫策略 |
| `/sitemap.xml` | 搜索引擎站点地图 |
| `/health` | 后端健康检查 |
| `/spots/:slug` | 城市/省份 SEO 落地页（37 个，如 `/spots/wuhan`、`/spots/xihu`） |
| `/about` | 方法论与数据说明 |
| `/llms.txt` | 面向 AI 爬虫的站点地图 |
| `/api/geo-stats` | GEO 爬虫统计（只读，基于独立 nginx 访问日志） |

未实现的早期规划接口：

- `/api/spot/xihu`
- `/api/forecast`
- `/api/predict`
- `/api/sun-times`

## 6. 当前验收状态

| 验收项 | 结果 |
|---|---|
| 自动化测试 | 109 / 109 通过 |
| 首页 | HTTP 200 |
| 西湖 API | HTTP 200 |
| 外滩 API | HTTP 200 |
| 全国站 API | HTTP 200 |
| 时间线 API | HTTP 200 |
| HTTPS | 正常 |
| Nginx 配置 | 正常 |
| systemd 后端 | Active |
| robots.txt | HTTP 200，`text/plain` |
| sitemap.xml | HTTP 200，有效 XML |
| 历史快照 | 1 天 |
| 实况反馈 | SQLite 存储闭环已上线（`/api/feedback/stats` 可聚合），等待日落后的真实样本 |
| Git 提交 | 已推送至 GitHub `main`（`laodoushouji/sunset-predict`，最新 `71f5027`） |
| Git 受管文件 | 40 基线 + 后续增量（SQLite、sharp、SEO 落地页等已纳入） |
| 手机端浏览器验证 | 390×844 下核心卡片等宽等高，10 站天气角标可见，详情天气正常 |

## 7. 已取消或被后续决策替代的需求

- 外滩锁定卡片：已被“外滩解锁并接入实时数据”替代。
- 外滩“算法调校中”提示：已删除。
- 三日趋势独立模块：已删除，日期预测改由滑动切换承担。
- 今日判断模块：已删除。
- 昨日实况照片模块：已删除；昨天历史数据仍保留。
- 页面底部图片来源行：已删除；版权页仍保留。
- 请喝咖啡 / Buy me a coffee：已替换为“为浪漫续航”。
- 支付宝：已移除，只保留微信。
- 强降雨仍保留 Quality：后续决策改为强降雨时 Quality 与 Probability 同时为 0。

## 8. 风险与技术债

### P1：下一阶段

1. **累计并分析实况真值**
   提交闭环已经上线，下一步需要累计样本，并按站点统计 Probability 命中率与 Quality 档位误差。

2. **补齐台风路径数据**
   厦门、深圳的台风外围奖金目前只有算法入口，没有真实数据源。

3. **修复 Git 远端** ✅ 已完成
   2026-08-14 已将全部本地提交推送至 GitHub `main`（`laodoushouji/sunset-predict`），形成异地源码备份。

4. **建立城市独立 SEO 页面** ✅ 已完成
   19 个城市 + 18 个省份共 37 个 `/spots/*` 可索引落地页已上线，Sitemap 扩至 38 条 `<loc>`，含直给摘要与原创摄影指南。

5. **提交搜索平台** ⏳ 仍待办
   robots.txt 和 Sitemap 只是基础条件，不等于已经被 Google 或百度收录；GSC/Bing/百度主动提交 sitemap 仍未做（需用户登录账号操作）。

### P2：产品化增强

6. **商业合作后台**
   将 `advertiserData` 从前端占位变量升级为按城市配置的后端数据。

7. **国内前端依赖加速**
    景区图片已经本地化，但 Tailwind CDN、Google Fonts、Lucide unpkg 仍可能影响国内首屏稳定性，可改成本地构建与自托管。

## 9. 推荐下一迭代

建议暂停新增城市，按以下顺序推进：

1. 连续累计历史快照与用户实况反馈，先达到每站至少 30 个有效样本。
2. 建立按站点、月份和天气类型统计的离线校准报告，不直接用少量样本修改线上分数。
3. ~~修复 GitHub 远端并推送~~ ✅ 已于 2026-08-14 完成（推送至 `main`）。
4. 接入厦门、深圳台风路径和下沉气流可信数据源。
5. 在 Google / Bing / 百度主动提交 `sitemap.xml`，扩大收录面（需用户登录平台操作）。

## 10. 下一次验收标准

- 普通月份 FIRE 占比目标为 5%–10%。
- GREAT 占比目标为 15%–25%。
- 所有站点在强降雨时评分与成功率均为 0。
- 每个站点累计至少 30 条有效实况反馈，并可与提交时的 V3 预测快照一一对应。
- 形成 Probability 命中率、Quality 平均绝对误差和各等级分布报告。
- GitHub 远端恢复可用，本地提交完成异地推送 ✅（2026-08-14 完成）。
- 19 个站点（及 18 省份）拥有独立可索引 URL，并进入 Sitemap ✅（37 个 `/spots/*` + 38 条 `<loc>`）。

---

本文档以当前代码、生产环境和本轮需求记录为准。动态天气分数会持续变化，不作为固定项目状态。
