# Sunset Predict · 19 城 SEO 城市页诊断与行动报告

> 目标：让 19 个独立城市页被 Google / 百度收录并带来**自然流量（organic）**。
> 当前日期：2026-07-27
> 结论先行：**技术上"19 页已建、可被抓取"已达标；真正拦路的是"内容太薄 + 19 页近重复 + 未提交验证"。** 其中只有"补内容 + 接验证 + 提交"是确定性可做的，最终收录与排名还需时间与外链。

---

## 一、现状盘点（好消息在哪）

| 维度 | 状态 | 证据 |
|---|---|---|
| 19 个独立 SSR 城市页 | ✅ 已建 | `server.mjs` 对 `/spots/:slug` 返回服务端整页 HTML 并注入 SEO（约 128–153 行） |
| 收录清单 | ✅ 已含全部 | `sitemap.xml` 含首页 + `xihu`/`waitan` + 17 个区域城（共 20 条 URL） |
| 抓取放行 | ✅ 已配 | `robots.txt` 放行并指向 sitemap，且已 `Disallow: /api/` |
| 每页独立元数据 | ✅ 已注入 | `seo.js:194-226` 生成独立 `title`/`description`/`canonical` + OG/Twitter |
| 结构化数据 | ✅ 已注入 | `TouristAttraction` + `GeoCoordinates` + `BreadcrumbList`（`seo.js:90-181`） |
| 服务端渲染正文 | ✅ 真实 HTML | Google/百度抓到的是带分数的真 HTML，非 JS 灌入 |
| 每城独立 OG 图 | ✅ 已配 | `city-${slug}.webp`（`seo.js:56-60`），19 张图已存在于 `frontend/assets/` |
| 部署自检 | ✅ 已校验 | CI 校验 `/spots/hongkong` 的 title/canonical/H2（`remote-deploy.sh`） |

**19 个城市页清单**（slug → 站点）：
xihu(杭州西湖)、waitan(上海外滩)、beijing(北京景山/故宫)、shenzhen(深圳人才公园)、guangzhou(广州塔/海心桥)、hongkong(香港维港)、dunhuang(敦煌鸣沙山)、erhai(大理洱海)、chongqing(重庆来福士/南山)、xiamen(厦门黄厝)、qingdao(青岛栈桥)、chengdu(成都金融城双塔)、huangshan(黄山光明顶)、wuhan(武汉黄鹤楼)、sanya(三亚椰梦长廊)、xian(西安城墙)、nanjing(南京玄武湖)、xiapu(霞浦东壁村)、wuxi(无锡鼋头渚)。

---

## 二、三大缺口（按杠杆排序，决定能否拿到自然流量）

### 🔴 缺口 1（最致命，排名拦路）：内容太薄、19 页近重复
- `renderSpotLanding`（`seo.js:228-243`）只产出约 5 行 HTML：一个 H2 + 一段话 + 今日分数 + 回首页链接。**正文 ≈ 200 字，且 70%+ 与全站共用外壳相同。**
- `spotDescription`（`seo.js:73-75`）只把 `spotName`/`window.name`/`bestSpot.name` 三个变量替换进同一句话模板。19 个城市的 `meta description` 几乎一模一样 → 在 Google 眼里是 **19 个近重复页（near-duplicate）**，会互相内耗、谁也排不上去，"北京 晚霞 预测""西湖 日落 时间"这类长尾词不会被认可。
- **数据其实都有**（`cities.js` 的 `CITY_SPOTS` 里每城都有 `bestSpot.desc`/`hook`/`window.name`/`traits`），但没渲染成每城独有的正文。

### 🟠 缺口 2（你字面要的"提交"）：验证标签未接、未提交
- 代码已支持 `GOOGLE_SITE_VERIFICATION` / `BAIDU_SITE_VERIFICATION` 两个 env，会被注入 `<meta>`（`seo.js:183-192`、`server.mjs:143-144`）。
- 但搜索 `deploy/` 目录 **0 命中**，线上部署未配置这两个值 → **当前线上页面没有验证标签**，Google/Baidu 站长平台无法验证所有权。
- 提交动作本身是手动的（需你的 Google / 百度账号），我可把"接线 + SOP"准备好，但最终点提交是你本人。

### 🟠 缺口 3：站外权重几乎为零
- 新站无外链，Google/Baidu 信任度低、收录慢。
- 你已有 `小红书推广文案.md`，正好可带链回 `/spots/xihu` 等页；另可投摄影类目录站。这块偏运营。

### 附加小项（建议顺手修）
- `sitemap.xml` **缺 `<lastmod>`**：已设 `changefreq` 但无最后修改时间，爬虫难以判断新鲜度。建议补上（或改为构建时生成）。
- 首页 `description` 只提"西湖、外滩"，可补"全国 19 城"增强主题相关性。

---

## 三、行动路线（谁来做、做什么）

### P0 · 补内容（唯一能真正带来排名与自然流量的改动，本地可改可测）
给 19 城各写一段**服务端渲染的原创摄影指南**，数据驱动、带长尾关键词。建议每页正文 800–1500 字，结构统一为：
1. **地理成因**：为什么这里出晚霞/火烧云（结合 `window.name` 西方窗口、`traits` 地形）。
2. **最佳机位详解**：展开 `bestSpot.desc`（如"景山万春亭俯拍故宫中轴线与黄琉璃瓦"）。
3. **最佳季节与时段**：结合纬度/气候，给出"几月最好、几点到"。
4. **拍摄 Tips**：相机参数、构图、蓝调衔接（呼应 `hook`，如"维港晚霞与城市亮灯预报"）。
5. **交通与周边**：到达方式 + 顺路景点。
6. **FAQ**：3–5 个本地高频问题（天然长尾词容器）。

实现方式：`renderSpotLanding` 改为读取一份"城市内容库"（可在 `cities.js` 或新建 `content/city-guides.js`）渲染成多段 HTML。内容库可由我起草初稿（基于已有的地理/机位数据 + 公开常识），你审校事实准确性。

**长尾关键词示例（低竞争、高意图）**：
- 北京："`北京 晚霞 预测`"、"`故宫 日落 时间`"、"`景山 拍 晚霞 机位`"
- 西湖："`西湖 日落 时间`"、"`杭州 晚霞 预测`"、"`苏堤 拍 火烧云`"
- 霞浦："`霞浦 滩涂 日落 摄影 机位`"、"`东壁村 日落 攻略`"
- 香港："`维港 日落 时间`"、"`香港 拍 晚霞 机位`"
- 敦煌："`鸣沙山 日落 时间`"、"`敦煌 沙山 落日 参数`"

### P1 · 接验证 + 提交（确定性可准备，提交需你账号）
1. **补 `lastmod`** 到 `sitemap.xml`（或改为构建脚本生成）。
2. **把两个验证 env 写进部署清单**：在部署 env（如 `/etc/modelhub.env` 或 `deploy/` 的 env 模板）加：
   ```
   GOOGLE_SITE_VERIFICATION=<从 GSC 拿的 token>
   BAIDU_SITE_VERIFICATION=<从百度资源平台拿的 token>
   ```
   然后重部署使 `<meta>` 生效。
3. **提交 SOP（可出一份 `SEO提交SOP.md`）**：
   - **Google Search Console**：添加属性 `sunsetpredict.cloud` → 用 meta 标签验证 → 提交 sitemap `https://sunsetpredict.cloud/sitemap.xml`。
   - **Bing Webmaster**：同上（Bing 也覆盖部分 Yahoo / Copilot 流量，免费且值得做）。
   - **百度搜索资源平台（ziyuan.baidu.com）**：添加站点 → 验证 → 提交 sitemap + 手动提交重要 URL。⚠️ 注意：百度对新站、尤其**无 ICP 备案**的域名收录更慢、权重更低，如需主攻国内流量建议评估备案。
   - **可选**：360 搜索 / 搜狗站长平台。

### P2 · 站外权重（运营）
- 小红书笔记带链回对应城市页（已有文案基础）。
- 摄影论坛（图虫、蜂鸟、500px）/ 本地旅游版块发带链内容。
- 提交垂类目录站。

---

## 四、成功衡量（上线后怎么看效果）
- **收录**：`site:sunsetpredict.cloud`（Google）、百度 site 命令，目标 19 城页全部被收录。
- **索引覆盖**：GSC "覆盖率"、百度"索引量"稳步上升。
- **自然流量**：GSC / 百度统计的"搜索展示量 + 点击量"，目标 P0 上线 4–8 周内有长尾词日均点击。
- **排名**：盯核心长尾词（如"西湖 日落 时间"）是否进入前 20。
- **建议加统计**：当前未见百度统计/GA 接入，提交前应先埋点（GA4 + 百度统计），否则无法量化自然流量。

---

## 五、分工小结
| 项 | 负责 | 能否现在做 |
|---|---|---|
| P0 内容补全（城市内容库 + 渲染改造） | 我起草 + 你审校 | ✅ 可现在动手 |
| P1 `lastmod` + 验证 env 接线 + SOP 文档 | 我 | ✅ 可现在动手 |
| P1 实际登录账号提交 | 你 | ❌ 需你账号 |
| P2 站外带链/目录 | 你（我可出文案） | ⏳ 运营 |

> 注意：按部署铁律，任何后端/前端改动都需本地完整跑通（build + 部署脚本校验）后再上 ECS。P0/P1 代码改动我会遵循该流程。
